# lab5
```
По 10:
  1. npm init -y
     npm install pixi.js
     npm install webpack webpack-cli --save-dev
     npm run build
  2. npm install axios
     node vk-test.js
  3. npm install axios
     node geo-test.js
  8. npm install dotenv cross-env --save-dev
     npm run start:local
     npm run start:dev
     npm run start:prod
По 11:
  4-9: npm init vue@latest   #+TS
       cd ...
       +src
       npm install
       npm run dev
```
